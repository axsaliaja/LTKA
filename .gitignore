# Dependencies
node_modules/

# Build output
backend/dist/
frontend/.next/
frontend/out/

# Env files (keep .env.example)
.env
.env.local
*.env
!.env.example

# face-api.js model weights are committed so Vercel can serve them at /models.
# (If you prefer to keep the repo light, ignore them again and host on a CDN.)

# Logs / OS
*.log
.DS_Store
Thumbs.db
