# Base URL of the backend API (Caddy HTTPS endpoint on EC2).
# After each sandbox rebuild the EC2 public IP changes, so update this in
# Vercel (Project Settings -> Environment Variables) and redeploy.
#
# Example: https://54-91-12-34.nip.io
NEXT_PUBLIC_API_URL=http://localhost:4000
